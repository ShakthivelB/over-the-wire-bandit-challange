So this level is quite intresting where we no need to file the password here we need to break in to system


So there is a ssh private key u can try login using that but it will log out so i tried analyzing the bandit25 using grep
Bandit26 /etc/passwd and it gave me usr/bin/showtxt where this perticulat file ran after shh login where this file had a script

#!/bin/sh
Export
TERM=linux
Exec more ~/text.txt
Exit 0

Where it will first run as sh not as bash and check what type of terminal it is so sh used less more and vi where this script
Uses more if u see clearly after the shh login so the text.txt file very small in length so its logs out from the session
Once we loggined so we need to srink the terminal we use the it will show --more-- in the bottom when we give enter it will
Get us to the next line, so while it is showing --more-- we need used flag called "v" which will take us to the "vim" once
We are in the vim we need to escape the vim and enter ":shell" it will take us to the bandit26 loggin

